window.SavannaPreviewConfig = {
  // Public repo link shown on the main CTA button.
  githubRepoUrl: "https://github.com/etherman-os/SavannaGuard",

  // Optional: direct API endpoint for waitlist submissions (expects JSON POST).
  // Example: "https://api.yourdomain.com/waitlist"
  waitlistEndpoint: "",

  // Optional fallback: if endpoint is empty, submissions go to FormSubmit via this email.
  // Example: "hello@yourdomain.com"
  waitlistEmail: "",
};